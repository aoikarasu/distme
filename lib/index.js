/**
 * @aoikarasu/distme - Distribution utilities for frontend projects
 */

const concatCss = require('./concat-css');
const concatJs = require('./concat-js');
const postDist = require('./postdist');

module.exports = {
    concatCss,
    concatJs,
    postDist
};
